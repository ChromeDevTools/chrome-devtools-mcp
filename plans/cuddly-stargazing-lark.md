# ログイン待機UX改善計画（追加改善）

## 背景
v1.0.12で基本的なログイン待機UX改善を実装済み：
- タイムアウト: 5分
- ブラウザ前面表示
- 15秒ごとの残り時間表示
- ログイン成功時の明確なフィードバック

## 追加課題
**問題**: 初回メッセージ後、最初の進捗表示まで15秒間何も表示されない
- ユーザーは「本当に監視しているの？」と不安になる
- 監視が始まったことが明示されていない

## 追加改善案

### 監視開始の即時フィードバック
waitForLoginStatus関数の開始直後に、監視が始まったことを明示するメッセージを表示：

```
👀 ログインを監視中... 検出でき次第すぐにお知らせします
```

これにより：
- 監視が実際に始まったことがわかる
- 検出されたらすぐ通知されることが明確
- 15秒待たなくても安心できる

## 対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `src/login-helper.ts:267` | 監視開始メッセージを追加 |

## 実装詳細

### login-helper.ts 修正（1行追加）

```typescript
export async function waitForLoginStatus(
  page: Page,
  provider: 'chatgpt' | 'gemini',
  timeoutMs = LOGIN_TIMEOUT_MS,
  onStatusUpdate?: (message: string) => void,
): Promise<LoginStatus> {
  const log = onStatusUpdate || console.error;
  const start = Date.now();
  let delay = 500;
  let lastProgressReport = 0;

  // Bring browser to front so user can see login page
  try {
    await page.bringToFront();
  } catch {
    // Ignore errors (page might be closed)
  }

  // 👀 即座に監視開始メッセージを表示（追加）
  log('👀 ログインを監視中... 検出でき次第すぐにお知らせします');

  while (Date.now() - start < timeoutMs) {
    // ... 既存のロジック
  }
}
```

## 期待される出力

```
🔐 ログインが必要です
📱 ブラウザウィンドウを開きました。ログインしてください
⏳ ログイン完了を自動検出します（最大5分待機）
💡 二段階認証もゆっくり対応できます

👀 ログインを監視中... 検出でき次第すぐにお知らせします  ← 新規追加（即座に表示）
⏳ ログイン待機中... 残り 4分45秒
⏳ ログイン待機中... 残り 4分30秒
（ユーザーがログイン完了）
✅ ログイン検出！処理を続行します
```

